#!/bin/sh
# postrm do tmjos-defaults — recompila dconf após remoção
set -e

case "$1" in
    remove|purge)
        if command -v dconf >/dev/null 2>&1; then
            dconf update 2>/dev/null || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
